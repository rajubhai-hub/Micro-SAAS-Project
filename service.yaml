apiVersion: v1
kind: Service
metadata:
  name: devops-node-service
spec:
  type: NodePort
  selector:
    app: devops-node
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007